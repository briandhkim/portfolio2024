const SectionHeading = ({ sectionTitle }) => {
    return (
        <h1 className="text-3xl font-bold tracking-tight sm:text-4xl underline decoration-4 underline-offset-[0.5px] decoration-primary">
            {sectionTitle}
        </h1>
    );
};

export default SectionHeading;
